# `NDArray` with Custom Types

This example project shows how to use a custom `Point` type within an `NDArray`, including how to
define operations to do arithmetic with `NDArray`s of `Point`s.

To run:

```bash
zig build run
```
