# Performing Operations with `NDArray`

This example project shows how to perform operations, such as arithmetic functions
and custom transformations, to an `NDArray` of a given numeric type.

To run:

```bash
zig build run
```
